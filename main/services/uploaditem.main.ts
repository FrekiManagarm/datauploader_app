import { ipcMain } from "electron";
import prisma from "../helpers/prisma";

ipcMain.on("get-uploaditem", async (event, arg) => {});

ipcMain.on("get-one-uploaditem", async (event, arg) => {});

ipcMain.on("create-uploaditem", async (event, arg) => {});

ipcMain.on("update-uploaditem", async (event, arg) => {});

ipcMain.on("delete-uploaditem", async (event, arg) => {});
